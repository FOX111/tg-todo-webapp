# 📝 Telegram TODO App

Современное TODO-приложение, интегрированное с **Telegram Mini App (WebApp)** для управления задачами прямо в мессенджере.

## 🎯 Основные возможности

- ✅ Добавление, редактирование и удаление задач
- 🔄 Переключение статуса выполнения задач
- 🗑️ Массовое удаление выполненных задач
- 📊 Статистика задач через главную кнопку Telegram
- 💾 Автоматическое сохранение данных
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Современный UI с плавными анимациями

## 📁 Структура проекта

```
tg-todo-app/
├── index.html          # Основная HTML-структура
├── script.js           # Логика приложения и Telegram API
├── style.css           # Стили и дизайн
├── README.md           # Документация
└── tg-todo-backend/    # Бэкенд (Express.js)
    ├── index.js        # API сервер
    └── package.json    # Зависимости
```

## 🔧 Технологии

### Frontend
- **HTML5** - структура приложения
- **CSS3** - современный дизайн с градиентами и анимациями
- **Vanilla JavaScript** - логика приложения
- **Telegram WebApp API** - интеграция с Telegram

### Backend
- **Node.js** - серверная среда выполнения
- **Express.js** - веб-фреймворк для API
- **CORS** - поддержка кросс-доменных запросов

## 💾 Хранение данных

### ⚠️ ВАЖНО: Текущая реализация

**На данный момент бэкенд НЕ используется в приложении!**

Приложение использует **двухуровневую систему хранения**:

1. **Telegram Cloud Storage** (основное хранилище)
   - Данные хранятся в облаке Telegram
   - Автоматическая синхронизация между устройствами
   - Доступно только при запуске через Telegram

2. **localStorage** (резервное хранилище)
   - Используется как fallback, если Telegram недоступен
   - Данные хранятся локально в браузере
   - Работает при тестировании вне Telegram

### Бэкенд API (готов, но не подключен)

Бэкенд реализован и готов к использованию, но требует интеграции с фронтендом:

**Endpoints:**
- `GET /todos?userId=<id>` - получить задачи пользователя
- `POST /todos` - сохранить задачи
  ```json
  {
    "userId": "123456",
    "todos": [
      { "id": 1234567890, "text": "Задача", "completed": false }
    ]
  }
  ```

## 🚀 Запуск проекта

### Frontend (клиентская часть)

Просто откройте `index.html` в браузере или разместите на веб-сервере:

```bash
# Простой способ - Python HTTP сервер
python3 -m http.server 8000

# Или Node.js http-server
npx http-server
```

Откройте в браузере: `http://localhost:8000`

### Backend (если потребуется)

```bash
cd tg-todo-backend
npm install
npm start
```

Сервер запустится на `http://localhost:3000`

## 🔗 Интеграция с Telegram

### Настройка Telegram Mini App

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Используйте команду `/newapp` для создания Mini App
3. Укажите URL, где размещен ваш `index.html`
4. Получите ссылку на приложение

### Telegram WebApp API

Приложение использует следующие возможности:

```javascript
// Инициализация
Telegram.WebApp.ready();
Telegram.WebApp.expand();

// Получение данных пользователя
const user = Telegram.WebApp.initDataUnsafe?.user;

// Cloud Storage
Telegram.WebApp.CloudStorage.setItem('todos', data);
Telegram.WebApp.CloudStorage.getItem('todos', callback);

// Главная кнопка
Telegram.WebApp.MainButton.setText('Готово');
Telegram.WebApp.MainButton.show();
```

## 📱 Функционал приложения

### Управление задачами

- **Добавление**: введите текст и нажмите "Добавить" или Enter
- **Выполнение**: кликните на чекбокс рядом с задачей
- **Удаление**: нажмите кнопку "×" справа от задачи
- **Очистка**: кнопка "Очистить выполненные" удаляет все отмеченные задачи

### Статистика

Нажмите на главную кнопку Telegram (внизу экрана) для просмотра статистики:
- Общее количество задач
- Количество выполненных задач
- Правильное склонение слова "задача"

### Отладка

Приложение включает встроенную систему диагностики для отслеживания:
- Доступности Telegram WebApp API
- Статуса Cloud Storage
- Данных пользователя
- Процесса сохранения/загрузки задач

## 🎨 Дизайн

- **Современный UI** с градиентами и тенями
- **Плавные анимации** при наведении и взаимодействии
- **Адаптивная верстка** для всех размеров экранов
- **Темная тема** (наследуется от настроек Telegram)
- **Accessibility** - поддержка клавиатурной навигации

## 🔒 Безопасность

- **XSS-защита** через `escapeHtml()` функцию
- **Валидация данных** на стороне сервера
- **CORS настроен** для безопасных запросов

## 📝 Структура данных

### Формат задачи

```javascript
{
  id: 1234567890,        // Timestamp в качестве уникального ID
  text: "Текст задачи",  // Описание задачи
  completed: false       // Статус выполнения
}
```

### Хранение

```javascript
// Массив всех задач пользователя
todos = [
  { id: 1234567890, text: "Купить молоко", completed: false },
  { id: 1234567891, text: "Выучить JavaScript", completed: true },
  { id: 1234567892, text: "Сделать TODO app", completed: true }
]
```

## 🔮 Возможные улучшения

- [ ] Подключить бэкенд для синхронизации между устройствами
- [ ] Добавить категории/теги для задач
- [ ] Реализовать приоритеты задач
- [ ] Добавить сортировку и фильтрацию
- [ ] Поддержка подзадач
- [ ] Напоминания через Telegram Bot
- [ ] Экспорт/импорт задач
- [ ] Темная/светлая тема (переключатель)
- [ ] История изменений задач
- [ ] Поиск по задачам

## 🤝 Разработка

### Добавление новых функций

1. Для изменения UI редактируйте `index.html` и `style.css`
2. Логика приложения находится в `script.js`
3. API endpoints можно расширить в `tg-todo-backend/index.js`

### Подключение бэкенда

Для использования бэкенда API вместо Cloud Storage:

1. Замените функции `saveTodos()` и `loadTodos()` в `script.js`
2. Используйте `fetch()` для запросов к API
3. Передавайте `userId` из `Telegram.WebApp.initDataUnsafe.user.id`

Пример:

```javascript
async function saveTodos() {
    const userId = tg.initDataUnsafe?.user?.id;
    await fetch('http://localhost:3000/todos', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId, todos })
    });
}
```

## 📄 Лицензия

ISC

---

**Создано для Telegram Mini Apps** 🚀
